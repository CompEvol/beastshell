// @see(doc/html/beast/base/Trig.html) Trigonometric Functions

tan(x) {
	if (x instanceof java.util.List || x.getClass().isArray()) {
		java.util.List<Double> result = new ArrayList<Double>();
		for (i : x) {
			result.add(java.lang.Math.tan(i));
		}
		return result;
	} else {
		return java.lang.Math.tan(x);
	}
}
