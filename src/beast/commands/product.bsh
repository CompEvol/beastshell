// @see(doc/html/beast/base/StatUtils.html)

product(sample) {
	double [] x = toDoubleArray(sample);
	return org.apache.commons.math3.stat.StatUtils.product(x);
}

product(sample, int begin, int end) {
	double [] x = toDoubleArray(sample);
	return org.apache.commons.math3.stat.StatUtils.product(x, begin, length);
}
